
/* getItem() on local storage to hold list data */
/* function getNewDataItems() {
  const store_list_data = localStorage.getItem('new_tasks');
  return [];
} */

/* inspect JSON data of local storage key value pairs */
/* function getRecentSearches() {
  const searches = localStorage.getItem('new_tasks');
  if (searches) {
    return JSON.parse(searches);
  }
  return [];
} */

/* clear the local storage data on button click */
/* clearButton.addEventListener('click', () => {
  localStorage.removeItem('new_tasks');
  recentSearchList.innerHTML = '';
}); */
