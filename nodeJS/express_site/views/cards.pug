extends layout.pug

block content
    h2 Flash Cards
    
    section#content
        h2= text

        if hint
            p 
                i Hint: #{hint}
        a(href=`${id}?side=${sideToShow}` class="switch_side")= sideToShowDisplay
        br
        a(href='/cards' class="next_card") Next Card!
    
