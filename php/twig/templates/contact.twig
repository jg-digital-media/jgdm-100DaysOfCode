{% extends "base.twig" %}

{% block title %}Contact.twig {% endblock %}

{% block content %}

<h1>This is the contact page</h1>

  {# import macro to access it #}
    {% import "forms.twig" as forms %}

<form method="post">

    {{ forms.input('name', post.name}}

    {{ forms.input('email', post.email, '', 'We will never share your email with anyone'}}
    <button type="submit">Submit</button>
</form>

{% endblock %}

{% block footer %}
    <footer>
        <p>&copy; {{ "now"|date('Y') }} Jonnie Grieve Digital Media</p>
    </footer>

    </body>
    </html>

{% endblock %}